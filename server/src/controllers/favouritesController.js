function getFavourites(req, res) {
  res.status(501).json({ message: 'Favourites not implemented yet' });
}

function saveFavourite(req, res) {
  res.status(501).json({ message: 'Save favourite not implemented yet' });
}

module.exports = {
  getFavourites,
  saveFavourite,
};

